- if @posts.any?
  %ul#posts_list
    - @posts.each do |post|
      %li.post{:class => posts_list_classes(@posts, post)}
        .title
          %strong= link_to h(post.title), post_path(post)
          by
          = h post.user.username
        .meta= content_tag :span, post.created_at.strftime('%B %e'), :title => post.created_at
- else
  No posts found!
