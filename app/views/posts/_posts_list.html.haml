- if @posts.any?
  %ul#posts_list
    - @posts.each do |post|
      %li{:class => "post#{first_or_last_post(post)}"}
        .title
          %strong #{link_to h(post.title), post_slug_url(post)} by #{post.user.username}
        .meta #{post.created_at.strftime('%B %d, %Y %H:%M')}
- else
  %p No posts found!
