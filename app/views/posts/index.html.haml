- title 'Posts'

#posts
  - @posts.each do |post|
    %h2
      = link_to h(post.title), post.url
      %span.comments= '(' + pluralize(post.comments.count, 'comment') + ')'
    .author
      by
      = post.user.username
      = time_ago_in_words(post.created_at)
      ago
    .content
      = simple_format(post.body)

- if can? :create, Post
  %p= link_to 'New post', new_post_path
