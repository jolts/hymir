- title 'Posts'

#posts
  - @posts.each do |post|
    %h2
      = link_to h(post.title), post
      by
      = post.user.username
    .content
      = simple_format(post.body)

- if can? :create, Post
  = link_to 'New post', new_post_path
